# Public Assets

这里放不需要经过 Vite 打包处理的静态资源。

推荐目录：

- `assets/`：图片、图标、静态 JSON、字体等资源

访问方式示例：

- `public/assets/logo.png` 在页面中访问为 `/assets/logo.png`
- `public/assets/data/tools.json` 在页面中访问为 `/assets/data/tools.json`
